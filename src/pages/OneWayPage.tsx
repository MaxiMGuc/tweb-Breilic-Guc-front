// Заглушка для режима поиска One-way из табов формы.
import SectionStub from '../components/SectionStub'

// Временная страница, куда пользователь попадает при выборе one-way режима.
function OneWayPage() {
  return (
    <SectionStub
      title="One-way flights"
      description="One-way search mode is under construction. Soon you will be able to find single-direction tickets here."
    />
  )
}

export default OneWayPage
